{
  "name": "patient-example",
  "version": "1.0.0",
  "description": "Patient - Blood Pressure Monitor, Thermometer & Symptoms",
  "type": "module",
  "scripts": {
    "build": "esbuild browser-sdk.js --bundle --format=esm --outfile=public/colonies-sdk.js",
    "start": "npm run build && node server.js"
  },
  "dependencies": {
    "colonies-ts": "file:../..",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "esbuild": "^0.19.0"
  }
}
